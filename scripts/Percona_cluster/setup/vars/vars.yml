---
#All variables which need for launch EC2 instances
AWS_ACCESS_KEY:
AWS_SECRET_KEY: 
keypair: 
# Add ssh_user like(centos/ubuntu) and AWS Private Key path
ansibleUser: ansible_ssh_user=centos ansible_ssh_private_key_file=

# Default variables for configurations
hostpath: /etc/ansible/hosts
StoreFilePath: ../vars/vars.yml
mysql_root_password: Cision@MYSQL #Password of MYSQL on all Percona Nodes

# All IP's of new Percona Instances
